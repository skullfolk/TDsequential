server {
    listen 80;
    server_name localhost;

    # Serve static frontend files
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html; # Handle React Router
    }

    # Proxy API requests to the backend service
    # Docker Compose: backend container = hostname 'backend'
    # Kubernetes:     backend service  = hostname 'quant-backend-svc'
    location /api/ {
        proxy_pass http://quant-backend-svc:80;  # K8s Service name (not Docker Compose hostname)
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
